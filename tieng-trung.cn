<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trang Code Test Hoàn Chỉnh</title>
    
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 50px;
            background-color: #e6f7ff; /* Nền xanh nhạt */
            color: #004085; /* Chữ xanh đậm */
            text-align: center;
            border: 2px solid #b8daff;
            padding: 20px;
        }
        h1 {
            color: #007bff; /* Màu xanh nổi bật */
        }
        .result {
            margin-top: 20px;
            font-weight: bold;
            font-size: 1.2em;
            color: #28a745; /* Màu xanh lá cây cho kết quả */
        }
    </style>

</head>
<body>

    <h1>✅ Mã Chạy Thử Đã Sẵn Sàng</h1>
    <p>Trang này đã tải thành công HTML và CSS. Bây giờ hãy kiểm tra JavaScript...</p>
    
    <div id="js-test" class="result">
        Đang chờ JavaScript chạy...
    </div>

    <script>
        // 1. Khai báo và tính toán
        let numA = 20;
        let numB = 15;
        let diff = numA - numB;

        // 2. Cập nhật nội dung trên trang web (DOM Manipulation)
        document.getElementById('js-test').innerHTML = 
            "**JavaScript hoạt động!** <br>" + 
            "Phép tính: " + numA + " - " + numB + " = " + diff;
            
        // 3. In ra Console (ấn F12 để kiểm tra)
        console.log("--- Báo cáo Test JavaScript ---");
        console.log("Biến diff = " + diff);
        console.log("Mọi thứ đều OK.");
        
        // 4. Hiển thị hộp thoại (alert) để xác nhận ngay lập tức
        alert("🎉 JavaScript Đã Chạy Thành Công! (20 - 15 = " + diff + ")");
    </script>

</body>
</html>
