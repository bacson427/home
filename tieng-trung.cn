<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Code Test Đổi Màu Chữ</title>
    
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 50px;
            background-color: #f0f0f0;
            text-align: center;
        }
        h1 {
            color: #333;
        }
        .container {
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 8px;
            display: inline-block;
            background-color: #fff;
        }
        #text-to-change {
            font-size: 1.5em;
            font-weight: bold;
            margin-top: 20px;
            transition: color 0.5s; /* Hiệu ứng chuyển màu mượt mà */
        }
        input[type="text"] {
            padding: 10px;
            margin-top: 15px;
            border: 2px solid #007bff;
            border-radius: 4px;
            font-size: 1em;
            text-align: center;
        }
    </style>

</head>
<body>

    <div class="container">
        <h1>Nhập Mã Màu Dưới Đây</h1>
        <p>Thử nhập: **red**, **blue**, **#008000**, hoặc **#FF6347**</p>
        
        <input type="text" id="color-input" placeholder="Nhập tên màu hoặc mã Hex" value="red">
        
        <div id="text-to-change">
            CHỮ NÀY SẼ ĐỔI MÀU
        </div>
    </div>

    <script>
        // Lấy các phần tử HTML
        const colorInput = document.getElementById('color-input');
        const textToChange = document.getElementById('text-to-change');

        // Hàm thay đổi màu chữ
        function changeTextColor() {
            const newColor = colorInput.value;
            // Gán màu mới cho thuộc tính 'color' của CSS
            textToChange.style.color = newColor;
            
            // In ra console để kiểm tra (bấm F12)
            console.log("Đã đổi màu chữ thành: " + newColor);
        }

        // Lắng nghe sự kiện "input" (mỗi khi gõ một ký tự)
        colorInput.addEventListener('input', changeTextColor);
        
        // Chạy lần đầu tiên để thiết lập màu mặc định
        changeTextColor(); 
    </script>

</body>
</html>
